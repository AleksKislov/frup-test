export const TIME_TO_WAIT = 1000; // milliseconds
export const INTERVAL = 200;
export const CONNECT_OPTS = { useNewUrlParser: true, useUnifiedTopology: true };

export const BATCH_SIZE = 1000; // number of documents to insert in one batch
